<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="">
  <meta name="author" content="">

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= javascript_tag do %>
    <%= yield :js %>
  <% end %>

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <title><%= app_name %></title>
</head>
<body class="<%= current_user.profile %> <%= controller_name %> <%= action_name %>" data-controller="base">
  <header class="navbar navbar-dark sticky-top bg-dark flex-md-nowrap p-0 shadow">
    <a class="navbar-brand col-md-3 col-lg-2 me-0 px-3 fs-6" href="/"><%= app_name %></a>

    <button class="navbar-toggler position-absolute d-md-none collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#sidebarMenu" aria-controls="sidebarMenu" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <%= form_tag clients_path, method: :get, class: 'w-100 p-0', data: { controller: 'search' } do %>
      <%= text_field_tag :q, params[:q], class: "form-control form-control-dark w-100 rounded-0 border-0", placeholder: "Pesquisar cliente..." %>
    <% end %>
    <div class="navbar-nav mx-2">
      <div class="nav-item text-nowrap">
        <%= link_to 'Sair',
                    destroy_user_session_path,
                    method: :delete,
                    class: 'nav-link px-3' %>
      </div>
    </div>
  </header>

  <div class="container-fluid">
    <div class="row">
      <%= render 'sidebar' %>

      <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
        <%= yield %>
      </main>
    </div>
  </div>
</body>
</html>
